package customLibrary;

public class DesignPatterns {

	/* THESE ARE JUST SNIPPETS OF CODE AND SO CANNOT BE COMPILED
	 * 
	 * 1E).
	 * To create regular line breaks during a loop, replace "X" with
	 * the desired number of loops executed per line:
	 * 
	 * 1D).
	 *   if (((x + 1) % X) == 0)
	 *			System.out.println();
	 *
	 * 
	 */
	
}
